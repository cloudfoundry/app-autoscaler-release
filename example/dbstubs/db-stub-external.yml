---
database:
  address: 10.0.2.2
  databases:
    - {name: autoscaler, tag: default}
  db_scheme: postgres
  port: 5432
  roles:
    - {name: postgres, password: postgres, tag: default}

instance_count_overrides:
  - name: postgres
    instances: 0
