---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: ghcr.io/cloudfoundry/app-autoscaler-release-deploy
    tag: main

params:

inputs:
- name: ci
- name: bbl-state

run:
  path: ci/ci/autoscaler/scripts/update-dns-servers.sh
