---
platform: linux

params:
  AUTOSCALER_DIR: app-autoscaler-release

inputs:
- name: app-autoscaler-release
- name: bbl-state

outputs:

run:
  path: app-autoscaler-release/ci/autoscaler/scripts/cleanup-autoscaler-deployments.sh
