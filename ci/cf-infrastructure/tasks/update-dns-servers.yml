---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: ghcr.io/cloudfoundry/app-autoscaler-release-tools
    tag: main

params:

inputs:
- name: ci
- name: bbl-state

run:
  path: ci/ci/cf-infrastructre/scripts/update-dns-servers.sh
